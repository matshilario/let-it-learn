[project]
name = "let-it-learn-backend"
version = "0.1.0"
description = "Let It Learn - SaaS platform for interactive learning"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"
line-length = 120
